#include <iostream>
#include <string>
using namespace std;

int main() {
    char char_guess;
    string guess="";
    int l=0;
    char arr[]="aran";
    int len=sizeof(arr);
    char guessed[len];
    int chances=0;
    while (chances < 6){
        cout<<"\n"<<chances<<"enter your guess: ";
        cin>>char_guess;
        if (word.find(char_guess) == std::string::npos) {
                chances++;
    }
        for (int i=0;i<len;i++){
        if(arr[i]==char_guess){
            guessed[i]=arr[i];
            
        }else{
            
        }
    }
}

}

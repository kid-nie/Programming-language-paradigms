#include <iostream>
#include <cmath>
#include <string>
using namespace std;

int main() {
    string line(40,'-');
    cout<<"7. Create an interest calculator that will prompt the user to input a starting balance (£1000) and an interest rate (5%) on the starting balance. Work out the balances after the first, second and third year."<<endl;
    
    float num;
    cout<<"enter your balance:";
    cin >>num;
    
    float in;
    cout<<"type your interest (w/o %):";
    cin >>in;
    
    for (int i=1;i<4;i++){
        float interest = (num * in * i) / 100;
        cout<<"year "<<i<<" interest: "<<interest<<endl;
        cout<<"year "<<i<<" balance: "<<interest+num<<endl;
        cout<<line<<endl;
    }
}

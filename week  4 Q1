#include<iostream>
using namespace std;

int main() {
    int arr[5] = { 10,20,30,40,50 } ;//int* ptr = arr;
    // Pointing to the first element of the array
    cout <<"Elements of the array using pointer arithmetic:" << endl;
    for (int i = 0; i < 5; i++) {
        cout << arr[i] <<""<<endl;
    }
return 0;
}

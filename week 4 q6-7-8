
//6. Write a C program that defines a struct for a student with fields name (string)
//and age (integer), uses malloc() to allocate memory for one student, takes user input for name and age, prints the student's information, and frees the allocated memory.

//7. Write a program that queries the user to enter a positive integer, then creates a
//std:list storing all integers from 1 to the user-generated number.
//8. During the lecture, two ADTs in the C++ standard template library were
//introduced, e.g., stack and list. Other ADTs in the standard template library
//(STL) include vector and queue. Similar to the code example in the lecture for
//list and stack, implement sample programs for vector and queue. Based on
//this and further research, identify the characteristics of vector and queue.

#include <cstdlib>
#include<iostream>
using namespace std;

string arrayname[2];
string arrayage[2];
char *p1;
int *p2;

int main() {
    int n=2;
    string name,age;
    for (int i=0;i<n;i++){
        cout << "Enter name  : ";
        cin >> name;
        
        cout << "Enter age  : ";
        cin >> age;
        
        arrayname[i]=name;
        arrayage[i]=age;
        
    }
    int len=sizeof(arrayname);
    
    p1= (char*)malloc(sizeof(char)*len);
        
    p2= (int*)malloc(sizeof(int)*2);
        
    for (int i=0;i<len;i++){
        *(p1+i)= name[i];
    }
    //for (int i=0;i<len2;i++){
        //  *(p2+i)= age[i];
    //}
    for (int i=0;i<len;i++){
        cout<<*(p1+i)<<"";
    }
        
    free(p1);
    free(p2);
    
    }
    
